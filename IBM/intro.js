var letter = "E" ;

if (letter != "") {
    if (letter == "A"){
        //Statements
    }
    else if (letter == "B") {
        //Statements 2
    }
    else {
        //Nichts von oben
    }
    
}
else { 
}

//INCREMENTING THE LOOP COUNTER

for(var i = minVal; i < maxVal; i++) {
    //Statement that execute each time the for() loop
    //cycles, as long as the condition is satisfied
}

